{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "entityId": {
      "type": "string",
      "description": "Supabase entity_id to confirm before allowing advertising analysis."
    },
    "confirmed": {
      "type": "boolean",
      "const": true,
      "description": "Must be true to explicitly confirm the resolved entity."
    }
  },
  "required": ["entityId", "confirmed"]
}
